# Optional: override source file directly
INVOICE_DATA_FILE=
PORT=8000

# Administrative Access Credentials (optional - defaults to local demo values if unset)
INVOICEGUARD_ADMIN_USERNAME=admin
INVOICEGUARD_ADMIN_PASSWORD=change_this_in_production
# Optional pre-hashed password (format: salt$hexdigest using PBKDF2-HMAC-SHA256)
# INVOICEGUARD_ADMIN_PASSWORD_HASH=
